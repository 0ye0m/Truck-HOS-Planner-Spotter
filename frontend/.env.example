# ── TruckHOS Planner — frontend environment ────────────────────────
# Base URL of the Django backend.
# Leave empty for local development (Vite proxies /api and /media to :8000).
# In production set it to the backend origin, e.g.:
# VITE_API_BASE_URL=https://truckhos-backend.onrender.com
VITE_API_BASE_URL=
